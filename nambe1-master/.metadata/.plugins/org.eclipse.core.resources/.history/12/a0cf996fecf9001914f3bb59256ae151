package phappiness;

import java.util.Comparator;

public abstract class SocialSupportComparator implements Comparator<Countries> {

	@Override
	public int compare(Countries country1, Countries country2) {
		if(country1.getSocialSupport()==null&&country2.getSocialSupport()==null)
		return 0;
		
	}

	
	
	

}
